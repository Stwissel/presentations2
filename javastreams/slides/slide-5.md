## Participating Java features

- Collection framework & `.stream()`
- `map(...)`
- `filter(...)`
- `collect(...)`
- `Optional` (We don't like null)
- functional programming
  - `Function<T, R> {...}`
  - `Predicate<T> {...}`
  - `Operator<T> {...}`
  - `Supplier<T> {...}`
